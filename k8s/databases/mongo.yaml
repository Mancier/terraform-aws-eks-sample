apiVersion: v1
kind: Service
metadata:
  name: mongo-svc
  namespace: databases
spec:
  selector:
    app: mongo
  ports:
  - port: 8081
    targetPort: 8081
    name: mongo-port
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pv-claim
  namespace: databases
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-dpt
  namespace: databases
  labels:
    app: mongo
spec:
  selector:
    matchLabels:
      app: mongo
  replicas: 2
  template:
    metadata:
      labels:
        app: mongo
    spec: 
      containers:
      - image: mongo:bionic
        name: mongodb
        ports:
        - name: mongo
          containerPort: 8081
          protocol: TCP
        env:
          - name:  MONGO_INITDB_ROOT_PASSWORD
            valueFrom:
              secretKeyRef:
                name:  mongodb-initdb-root-password
                key:  password
        volumeMounts:
        - name: mongo-persistent-storage
          mountPath: /data/db
      volumes:
      - name: mongo-persistent-storage
        persistentVolumeClaim:
            claimName: mongo-pv-claim